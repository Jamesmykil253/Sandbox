// Team.cs (v1.1 - No changes from v1.0)
namespace Platformer
{
    // This enum defines the possible teams in the game. By putting it in its own file,
    // any other script in our project can easily access and use it.
    public enum Team { Home, Away, Neutral }
}